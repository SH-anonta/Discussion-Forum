All unit tests of this app 'forum' are in this package

How tests are organized in this package:

tests are grouped into files based on what they test

UTs (Unit Tests) that assess models are in files that have name ending with view
UTs (Unit Tests) that assess views are in files that have name ending with model

tests of views that interact with certain models also separated in different files

